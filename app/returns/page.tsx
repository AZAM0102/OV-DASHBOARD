import{CheckCircle2,Clock3,XCircle}from"lucide-react";import{AppShell}from"@/components/layout";import{orders}from"@/lib/data";import{MarketplaceBadge}from"@/components/ui";import{PageHeader}from"@/components/page-header";
export default function Returns(){const rows=orders.filter(o=>o.status==='Returned'||o.status==='Pending').slice(0,6);return <AppShell><div className="mx-auto max-w-[1500px] p-5 md:p-7"><PageHeader title="Returns" description="Track and resolve return requests from all three marketplaces."/><div className="mb-4 grid grid-cols-1 gap-3 md:grid-cols-3">{[['Pending Returns','18',Clock3],['Approved','9',CheckCircle2],['Rejected','5',XCircle]].map(([a,b,I])=><div className="ov-card p-5" key={a as string}><div className="flex items-center gap-3"><I className="text-[#ff7a00]"/><div><span className="block text-xs text-slate-500">{a as string}</span><b className="text-2xl text-[#10284f]">{b as string}</b></div></div></div>)}</div><div className="ov-card overflow-hidden"><div className="border-b border-slate-100 px-5 py-4"><h2 className="font-extrabold text-[#10284f]">Return Requests</h2></div><div className="ov-scroll overflow-x-auto"><table className="w-full min-w-[900px] text-sm"><thead className="bg-[#f7f9fc] text-left text-xs text-slate-500"><tr><th className="px-5 py-3">Return ID</th><th>Order</th><th>Marketplace</th><th>Product</th><th>Reason</th><th>Amount</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map((o,i)=><tr key={o.id} className="border-t border-slate-100"><td className="px-5 py-3 font-semibold">#RET-{820+i}</td><td>{o.id}</td><td><span className="flex items-center gap-2"><MarketplaceBadge marketplace={o.marketplace}/>{o.marketplace}</span></td><td>{o.product}</td><td>{['Size / Fit','Damaged','Wrong product'][i%3]}</td><td className="font-semibold">₹{o.amount.toLocaleString('en-IN')}</td><td><span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-700">Pending</span></td><td><button className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold">Review</button></td></tr>)}</tbody></table></div></div></div></AppShell>}
