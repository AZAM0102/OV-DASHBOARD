"use client";import{Download,Printer,Truck}from"lucide-react";import{useState}from"react";import{AppShell}from"@/components/layout";import{orders}from"@/lib/data";import{MarketplaceBadge,StatusBadge}from"@/components/ui";import{PageHeader}from"@/components/page-header";
export default function Shipping(){const ready=orders.filter(o=>o.status==='Pending'||o.status==='Processing');const[sel,setSel]=useState<string[]>([]);return <AppShell><div className="mx-auto max-w-[1500px] p-5 md:p-7"><PageHeader title="Shipping & Labels" description="Prepare, generate and manage shipping labels in bulk." action={<button className="flex h-11 items-center gap-2 rounded-xl bg-[#ff7a00] px-4 text-sm font-bold text-white"><Printer size={17}/>Generate Labels</button>}/><div className="grid grid-cols-2 gap-3 md:grid-cols-4">{[['Ready to Ship','128'],['Labels Generated','94'],['Picked Up','76'],['In Transit','42']].map(x=><div className="ov-card p-5" key={x[0]}><span className="text-xs text-slate-500">{x[0]}</span><b className="mt-1 block text-2xl text-[#10284f]">{x[1]}</b></div>)}</div><div className="mt-4 ov-card overflow-hidden"><div className="flex flex-wrap justify-between gap-3 border-b border-slate-100 p-4"><span className="text-sm font-semibold">{sel.length} selected</span><div className="flex gap-2"><button className="flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-4 text-sm font-semibold"><Download size={16}/>Download Labels</button><button className="flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-4 text-sm font-semibold"><Truck size={16}/>Schedule Pickup</button></div></div><div className="ov-scroll overflow-x-auto"><table className="w-full min-w-[850px] text-sm"><thead className="bg-[#f7f9fc] text-left text-xs text-slate-500"><tr><th className="px-5 py-3"/><th>Order</th><th>Marketplace</th><th>Product</th><th>Amount</th><th>Status</th><th>Label</th></tr></thead><tbody>{ready.map(o=><tr key={o.id} className="border-t border-slate-100"><td className="px-5 py-3"><input type="checkbox" checked={sel.includes(o.id)} onChange={()=>setSel(v=>v.includes(o.id)?v.filter(x=>x!==o.id):[...v,o.id])}/></td><td className="font-semibold">{o.id}</td><td><span className="flex items-center gap-2"><MarketplaceBadge marketplace={o.marketplace}/>{o.marketplace}</span></td><td>{o.emoji} {o.product}</td><td>₹{o.amount}</td><td><StatusBadge status={o.status}/></td><td><button className="rounded-lg bg-[#10284f] px-3 py-2 text-xs font-semibold text-white">Generate</button></td></tr>)}</tbody></table></div></div></div></AppShell>}
